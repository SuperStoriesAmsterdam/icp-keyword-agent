// Terminal entry point — kept for local CLI use.
// The primary interface is now the web dashboard (dashboard/server.ts).
// To run locally: npx tsx dashboard/server.ts

console.log("Use the web dashboard instead: npx tsx dashboard/server.ts");
console.log("Or deploy to agent.superstories.com via Coolify.");
